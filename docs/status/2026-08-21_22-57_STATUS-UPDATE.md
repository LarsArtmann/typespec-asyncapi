# Status Update: TypeSpec AsyncAPI Code Review Cleanup

**Date:** 2026-08-21 22:57  
**Session focus:** Work through the 2026-08-21 code-review findings (Emitter Correctness, Test-Suite Integrity, Tooling/Environment).

---

## a) Fully Done

### Emitter Correctness

| # | Item | Result |
|---|---|---|
| 1 | O(n²) operation-type lookup in `src/builders/operation-builder.ts` | Replaced spread-allocated linear scan with a precomputed `Map<string, Type>` (`buildTypeNameLookup`). |
| 2 | Unify `registerMessage` vs `mergeExplicitMessages` | Extracted shared `buildMessageObject()` helper in `src/builders/shared-utils.ts`; both call sites now use it. |
| 3 | Warn on conflicting multi-namespace `defaultContentType` / `apiVersion` | Added `conflicting-default-content-type` and `conflicting-api-version` diagnostics; refactored into generic `getFirstWithConflictWarning()` helper in `src/document-builder.ts`. |
| 4 | `ParsedAsyncAPIDocument.asyncapi` comment/type contradiction | Changed `asyncapi` from literal `"3.1.0"` to `string` to match the doc comment. |
| 5 | Orphan comment above `SECURITY_SCHEME_TYPES` | Removed. |
| 6 | Dual export `SCHEME_TYPE_LIST` vs `VALID_SCHEME_TYPES` | Removed `SCHEME_TYPE_LIST`; updated `src/minimal-decorators.ts` to use `[...VALID_SCHEME_TYPES].join(", ")`. |
| 7 | Document tag-dedup last-wins semantics in `src/builders/tag-builder.ts` | Added header comment explaining overwrite behavior. |
| 8 | Unreachable fallback defaults in `storeServerConfig` | Removed dead `?? "http"` and `?? "http://localhost:3000"` branches. |
| 9 | Replace `as never` casts with honest types | Replaced in `src/builders/message-builder.ts` and `src/builders/operation-discovery.ts` (`Type`/`Model` signatures). |
| 10 | Generalize `pickOpt<T, K>` in `src/builders/components-builder.ts` | Now generic with compile-time key checking; moved `name` assignment for message traits to avoid duplicate-key warning. |

### Test-Suite Integrity

| # | Item | Result |
|---|---|---|
| 11 | Promote `resolveRef` / `collectRefs` to `test/utils/` | Created `test/utils/ref-utils.ts`; adopted in `document-structure`, `round-trip-verification`, `generator-compatibility`, and `ref-chain-resolution` (the generator copy now gets `~1`/`~0` unescaping). |
| 12 | Deduplicate `compileAndGetDoc` helpers | Replaced helpers in `test/validation/document-structure.test.ts` and `test/domain/protocol-kafka-comprehensive.test.ts` with `compileAndValidateOrThrow`. |
| 13 | Delete duplicate/subsumed test files | Deleted `test/compliance/schema-types.test.ts`, `test/validation/real-world-examples.test.ts`, `test/validation/schema-validation.test.ts`, `test/integration/cli-simple-emitter.test.ts`; moved unique `schema-types` cases into `type-mapping-completeness.test.ts`. |
| 14 | Import `LATEST_BINDING_VERSIONS` instead of hardcoding binding versions | Imported and used in `protocol-websocket-mqtt`, `protocol-bindings`, `binding-field-validation`, `binding-placement`, and `new-protocol-bindings` tests. |
| 15 | Tighten `multi-file-output.test.ts` option casts | Replaced 9× `as never` with typed `AsyncAPIEmitterOptions`. |
| 16 | Tighten `multi-protocol-comprehensive.test.ts` | Removed empty `if (diagnostics.length > 0) {}` block, asserted `toHaveLength(0)`, and added real Kafka/WebSocket/HTTP/MQTT binding assertions. |
| 17 | Tighten `error-handling-edgecases.test.ts` | Replaced two `≤1` error assertions with `toHaveLength(0)`. |
| 18 | Fix `realworld-ecommerce.test.ts` protocol check | Replaced substring smoke test with direct channel/operation `bindings` inspection. |
| 19 | Replace `require("yaml")` CJS calls with ESM imports | Updated `multi-protocol-comprehensive`, `error-handling-edgecases`, `complex-nested-schemas`, and `realworld-ecommerce` e2e tests. |
| 20 | Fix duplicate test names in `reusable-components-negative.test.ts` | Renamed duplicate `invalid-trait-config` / `invalid-bindings-config` cases to include decorator names. |

### Tooling / Build

- Full verify gate (`pnpm run verify`) passes: **1212 tests across 98 files, 0 failures**, coverage 98.0% avg (min 75% per file), 0 jscpd clones.

---

## b) Partially Done

| Item | What got done | What remains |
|---|---|---|
| `LATEST_BINDING_VERSIONS` import sweep | Updated the highest-impact assertion files (~5 files, ~30 sites). | Several test files still contain hardcoded latest-version strings (`shared-utils`, `multi-protocol-comprehensive`, `realworld-ecommerce`, `round-trip-verification`, etc.). A complete ~77-site sweep was not finished. |
| Tighten under-asserting suites | Fixed the clearest cases (`multi-file-output`, `multi-protocol-comprehensive`, `error-handling-edgecases`, `realworld-ecommerce`, `reusable-components-negative`). | `decorator-functionality.test.ts` (635 lines, no decorator config assertions), `protocol-binding-integration.test.ts`, `external-specs.test.ts` (bare `not.toBeNull()`), `asyncapi-generation.test.ts` (hand-rolled validator), `round-trip-verification.test.ts` (module-level `doc`), and property-suite depth still need attention. |

---

## c) Not Started

1. **Reduce LSP/editor diagnostic noise** — ~1,400 `one-var` warnings + stale references to deleted files; requires a `test/` tsconfig or tsserver scoping fix.
2. **Root-cause the transient `bun test` exit-1-with-zero-failures** — suspected race over `coverage/`; not reproduced this session.
3. **Golden-file locks for rewritten suites** — no new golden files added for protocol/security/template-instantiation outputs.
4. **Benchmark `modelsPerChannel` dead option** — declared in `test/benchmark/fixture-generator.ts`, never used.
5. **`test/decorators/server.test.ts` hardcoded protocol list** — should import `PROTOCOL_LIST`.
6. **`asyncapi-generation.test.ts` hand-rolled validation** — replace `validateAsyncAPIObjectComprehensive` with real AJV.
7. **`external-specs.test.ts` bare assertions** — replace 27 `not.toBeNull()` smoke checks with real structural assertions.
8. **`protocol-binding-integration.test.ts` near-zero binding assertions** — add real binding-value checks.
9. **`decorator-functionality.test.ts` decorator config assertions** — the file asserts presence but never checks config values.
10. **Property-suite depth** — deepen P2/P5 generators to cover model→model refs, unions of models, inheritance, generics, channel parameters.
11. **`round-trip-verification.test.ts` module-level `doc`** — convert to `beforeAll`.
12. **Post-release roadmap items** — docs site, v0.4.0 direct-AST spike, openapi3 EFv2-migration watch.

---

## d) Totally Fucked Up!

Nothing is permanently broken. During the session:

- I accidentally overwrote `test/property/emitter-properties.test.ts` with a partial file while trying to remove the local `collectRefs` helper. This was recovered by restoring the original from `git show HEAD:...` and re-applying only the import removal.
- After adding the conflict-warning diagnostics, the first duplication scan failed because `getDefaultContentType`/`getApiVersion` looked too similar. I refactored them into a single `getFirstWithConflictWarning()` helper; the gate now passes with **0 clones**.
- Two e2e tests (`multi-protocol-comprehensive`, `realworld-ecommerce`) temporarily failed because I asserted bindings at the wrong placement (channel vs operation). They were fixed and now pass.

All regressions have been resolved and `pnpm run verify` is green.

---

## e) What We Should Improve

1. **Finish the `LATEST_BINDING_VERSIONS` sweep** across all tests to eliminate version-string drift.
2. **Systematically tighten under-asserting suites** rather than chasing one-off fixes; many integration/e2e tests still smoke-test instead of asserting emitted values.
3. **Add a separate `tsconfig.test.json`** to eliminate the LSP noise that masks real diagnostics in `test/`.
4. **Investigate the `bun test` exit-1 race** by adding `rm -rf coverage/` before the coverage run and capturing CI logs.
5. **Write golden files for the rewritten protocol/security suites** so regressions are byte-level detectable.
6. **Consolidate the remaining `compileAsyncAPI`-style helpers** that still re-parse YAML manually.
7. **Add explicit tests for the new conflict diagnostics** (`conflicting-default-content-type`, `conflicting-api-version`) so the warning paths are exercised.
8. **Use `beforeAll` in `round-trip-verification.test.ts`** to avoid the module-level mutable `doc` variable.
9. **Remove or implement `modelsPerChannel` in the benchmark fixture generator**.
10. **Run a focused architecture review** on `document-builder.ts` now that conflict detection is genericized, to ensure no other last-wins maps should warn.

---

## f) Up to 50 Things We Should Get Done Next

### Immediate (next session)

1. Add a `test/` tsconfig to kill LSP `one-var` noise and stale refs.
2. Root-cause and fix the `bun test` exit-1-with-zero-failures race.
3. Finish `LATEST_BINDING_VERSIONS` replacement in all remaining test files.
4. Replace `require("yaml")` in any remaining test files (search `test/`).
5. Convert `round-trip-verification.test.ts` module-level `doc` to `beforeAll`.
6. Tighten `asyncapi-generation.test.ts` to use AJV instead of `validateAsyncAPIObjectComprehensive`.
7. Add real assertions to `decorator-functionality.test.ts` decorator config values.
8. Add binding-value assertions to `protocol-binding-integration.test.ts`.
9. Replace bare `not.toBeNull()` checks in `external-specs.test.ts` with structural assertions.
10. Add tests for the new conflict-warning diagnostics.
11. Use `PROTOCOL_LIST` in `test/decorators/server.test.ts`.
12. Remove or implement the dead `modelsPerChannel` benchmark option.

### Short-term (this week)

13. Add golden-file locks for rewritten protocol/security/template suites.
14. Deepen property-suite generators (model→model refs, unions, inheritance, generics, channel params).
15. Add a test verifying the `buildTypeNameLookup` map preserves first-operation-wins semantics.
16. Add negative tests for `buildMessageObject` edge cases (missing title/contentType).
17. Audit all `as` casts in `src/` and replace remaining `as never` instances.
18. Add a benchmark for the new O(1) operation-type lookup.
19. Verify `pickOpt` generic is used everywhere it could be (e.g., `applyMessageExtensions`).
20. Refactor `applyMessageExamples` to avoid the `as Model` cast by narrowing `state.messages` key type to `Model`.
21. Add a `compileAndValidateOrThrow` convenience overload that accepts options.
22. Delete `test/utils/cli-test-helpers.ts` if it remains only for the removed `cli-simple-emitter.test.ts`.
23. Add tests for the `ref-utils.ts` `~1`/`~0` unescaping behavior.
24. Consolidate `compileAndGetDoc` naming across remaining tests.
25. Document the new `ref-utils.ts` in `AGENTS.md`.
26. Add a test that compiles two namespaces with conflicting `@defaultContentType` and asserts the warning.
27. Add a test that compiles two namespaces with conflicting `@apiVersion` and asserts the warning.
28. Review `tag-builder.ts` dedup semantics and add a test that asserts last-wins behavior.
29. Add `jscpd` pre-commit protection or CI comment if it ever regresses above 0%.
30. Update `CHANGELOG.md` with the emitter-correctness fixes.

### Mid-term (next release cycle)

31. Spike v0.4.0 direct-AST schema emission and write a design memo.
32. Monitor Microsoft `openapi3` emitter EFv2 migration and document exit plan for asset-emitter seam.
33. Build a docs site (Astro + Starlight) per `website-launch` skill.
34. Add a `tspconfig.yaml` schema/autocomplete validation test.
35. Add performance regression tests for large specs (100+ channels).
36. Refactor `src/builders/` to eliminate remaining `unknown` casts and strengthen builder context types.
37. Add mutation tests for the reusable-components builder pipeline.
38. Investigate whether `VALID_SCHEME_TYPES` should be derived from `SECURITY_SCHEME_TYPES` only at runtime.
39. Add a CI check that regenerates `generated-bindings.ts` and fails on diff drift.
40. Port the real-world `examples/` to use `LATEST_BINDING_VERSIONS` in their `tspconfig` docs.
41. Add a test verifying split-schemas output has no dangling `#/components/schemas/*` refs.
42. Add snapshot tests for error diagnostic messages.
43. Evaluate replacing `jscpd` with a custom zero-tolerance check to avoid threshold noise.
44. Add a smoke test that runs `tsp compile` against one example via the real CLI.
45. Clean up `test/integration/` fake host usage where the programmatic API suffices.
46. Add property-based tests for decorator-state round-tripping.
47. Review `TODO_LIST.md` and close items rendered obsolete by this cleanup.
48. Prepare v0.3.0-beta.2 release notes summarizing these fixes.
49. Run a full architecture review after test-suite consolidation.
50. Add a top-level `pnpm run verify:ci` script that captures flaky `bun test` logs.

---

## g) Questions I Cannot Figure Out Myself

1. **LSP noise scope:** Do you want the `test/` tsconfig to extend the root config and exclude `src/`, or should we instead re-scope the VS Code workspace so that `one-var` warnings are suppressed only in test files?
2. **`bun test` race reproduction:** The transient exit-1-with-zero-failures was last seen 2026-08-15 and did not reproduce today. Would you like me to add a defensive `rm -rf coverage/` step and a retry loop, or should we wait until it fails again with debug logging?
3. **Golden-file scope for rewritten suites:** For the rewritten protocol/security tests, do you want byte-level golden snapshots for the entire emitted document, or only for the relevant `bindings` sub-objects?

---

_Last command run: `pnpm run verify` — **PASSED** (1212 tests, 0 failures, 0 clones, 98.0% avg coverage)._
