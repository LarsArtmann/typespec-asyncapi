# Status Report: AsyncAPI 3.1.0 Upgrade — Brutal Self-Review

**Date:** 2026-07-21 15:56 CEST
**Session scope:** Execute the 3.0.0 → 3.1.0 upgrade plan
**Verdict:** Functionally COMPLETE but INCOMPLETE in scope. Tests pass. Customer-facing artifacts were missed.

---

## TL;DR

I executed the **approved plan** (19 tasks) and all tests pass (406/0). But I **blindly trusted the plan's scope** instead of independently auditing the full repository. The plan said "src/ test/ AGENTS.md FEATURES.md README.md ROADMAP.md package.json" — I changed exactly those and declared victory. A 30-second `rg '3\.0' .` at the end would have caught **14 more references in 10 files** I completely missed: `examples/` (9 files) and `CHANGELOG.md` (3 refs). I also left the **planning document itself stale** (still says "Status: PLANNED").

> **Update 2026-07-22:** All P0 items resolved. The 14 missed references in `examples/` and `CHANGELOG.md` were fixed in commit `7376d34` ("sync all docs and examples with AsyncAPI 3.1.0 upgrade"). The planning doc status was updated. The only remaining `rg '3\.0'` hit is `bindingVersion: "0.3.0"` in `examples/advanced/advanced-decorators.tsp` — an HTTP binding version, NOT a spec version (false positive). Section (e) item 1 (consolidate version constants) was resolved by deleting `src/constants/index.ts` entirely (commit `42ad7ac`). Test count grew from 406 to **510**.

---

## a) FULLY DONE

| #   | Item                                                     | Evidence                                                                                                                                  |
| --- | -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Atomic version constant + type literal change            | `ASYNCAPI_SPEC_VERSION = "3.1.0"` in `document-builder.ts:31`, type literal in `asyncapi-document.ts:183` — compiled first try            |
| 2   | All validator schema paths → `3.1.0-without-$id.json`    | 4 files: `schema-validation.test.ts`, `all-examples-validation.test.ts`, `real-world-examples.test.ts`, `e2e/realworld-ecommerce.test.ts` |
| 3   | Golden file updated                                      | `ecommerce.expected.yaml:1` now `asyncapi: 3.1.0`                                                                                         |
| 4   | 151 test assertions bulk-replaced                        | `toBe("3.0.0")` → `toBe("3.1.0")` across all test files — grep returns zero hits                                                          |
| 5   | Test utility runtime guards updated                      | `cli-test-helpers.ts:60`, `type-guards.ts:55-56` — hardcoded `!== "3.0.0"` checks that would reject valid 3.1.0 output                    |
| 6   | BDD feature files + step definitions                     | `core-typespec-to-asyncapi.feature` (3 refs), `world.ts` (3 refs), `user-behaviors.test.ts` (4 refs)                                      |
| 7   | Dead constants in `src/constants/index.ts`               | 5 values: `ASYNCAPI_VERSION`, `VERSIONS.CURRENT/SUPPORTED/LATEST`, `DEFAULT_CONFIG.version`                                               |
| 8   | Diagnostic message in `lib.ts:9`                         | `"Only 3.0.0"` → `"Only 3.1.0"`                                                                                                           |
| 9   | Source header comments                                   | `emitter.ts`, `document-builder.ts`, `asyncapi-document.ts`, `protocols.ts`                                                               |
| 10  | Core docs: AGENTS.md, README.md, FEATURES.md, ROADMAP.md | Badge, example output, all "AsyncAPI 3.0" → "AsyncAPI 3.1"                                                                                |
| 11  | `package.json:5` description                             | `"AsyncAPI 3.0"` → `"AsyncAPI 3.1"`                                                                                                       |
| 12  | False positives correctly EXCLUDED                       | `test/bdd/package.json` (`^13.0.0` cucumber), `bindingVersion: "0.3.0"`, `@typespec/compiler: ^1.13.0`                                    |
| 13  | Build passes                                             | `bun run build` — 0 TS errors                                                                                                             |
| 14  | Lint passes                                              | `bun run lint` — 0 ESLint warnings                                                                                                        |
| 15  | Full test suite passes                                   | 406 pass, 0 fail, 2138 expect() calls                                                                                                     |
| 16  | Dependency pin unchanged                                 | `@asyncapi/specs` still `6.11.1` (no malware exposure)                                                                                    |

---

## b) PARTIALLY DONE

| #   | Item                                   | What's done                                                          | What's missing                                                                                                                         |
| --- | -------------------------------------- | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Repository-wide 3.0 purge**          | src/, test/, 4 core docs, package.json                               | **examples/ (9 files, 13 refs) and CHANGELOG.md (3 refs) NOT touched** — see section (c)                                               |
| 2   | **"Zero 3.0 references" verification** | Ran `rg '3\.0'` on scoped paths                                      | Verification scope was too narrow — should have been `rg '3\.0' .` from repo root                                                      |
| 3   | **Test naming consistency**            | Updated describe/it names in validation, integration, e2e, bdd tests | Did not audit whether ALL test descriptions mentioning "3.0" were caught (no comprehensive grep for `AsyncAPI 3.0` in string literals) |

---

## c) NOT STARTED

| #   | Item                                                            | Impact                       | Why it matters                                                                                                                                                                                           |
| --- | --------------------------------------------------------------- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **`examples/` directory — 9 files with 13 references to "3.0"** | **HIGH** — customer-facing   | Examples are what users read first. `examples/simple/expected-output.yaml` literally shows the wrong version. `examples/real-world/README.md` has 4 refs. `.tsp` source files in examples reference 3.0. |
| 2   | **`CHANGELOG.md` — 3 references to "3.0"**                      | **MEDIUM** — customer-facing | Changelog is the canonical record of what version this targets. Still says 3.0.                                                                                                                          |
| 3   | **Planning doc status update**                                  | **LOW-MEDIUM** — internal    | `docs/planning/2026-07-21_15-22_ASYNCAPI-3.1.0-UPGRADE.md:4` still says `**Status:** PLANNED (awaiting execution approval)`. I created this drift.                                                       |
| 4   | **Commit**                                                      | N/A                          | User did not explicitly say "commit". Correctly deferred per rules. 52 modified files sitting in working tree (includes pre-existing changes from prior session).                                        |
| 5   | **Coverage gate run**                                           | LOW                          | `scripts/coverage-gate.ts` (75% per-file minimum) was not run. Tests pass but coverage threshold not verified.                                                                                           |
| 6   | **Manual output inspection**                                    | LOW                          | Did not compile a sample `.tsp` and visually confirm `asyncapi: "3.1.0"` in generated JSON. Tests cover this but no human eyeball verification.                                                          |

---

## d) TOTALLY FUCKED UP

| #   | What                                                                     | Why it's bad                                                                                                                                                                                                                                                                                                                                                                                                 | Root cause                                                                                                                                                                                                 |
| --- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Verification scope was dangerously narrow**                            | I ran `rg '3\.0' src/ test/ AGENTS.md FEATURES.md README.md ROADMAP.md package.json` — exactly the files the plan listed. I never ran `rg '3\.0' .` from the repo root. **14 references in 10 files survived.**                                                                                                                                                                                              | I trusted the plan's scope enumeration instead of doing an independent full-repo audit. The plan was written by me in a prior session — it was an assumption, not ground truth.                            |
| 2   | **Declared "zero 3.0 references" in the final report when it was FALSE** | My final message said _"Zero AsyncAPI 3.0 references remain"_ and _"Zero `3.0` refs in src/ test/ docs — PASS"_. This was **technically true for the paths I checked** but **false for the repository**. The examples/ and CHANGELOG.md refs mean the statement was misleading.                                                                                                                              | I scoped my truth claim to match my search scope instead of matching reality. This is a form of lying-by-omission even though unintentional.                                                               |
| 3   | **Did not update the planning document**                                 | The planning doc I created in the prior session still says "Status: PLANNED". I literally read this file at the start of this session (line 4) and still didn't update it after execution.                                                                                                                                                                                                                   | Tunnel vision on execution tasks. The 19-task todo list didn't include "update the plan doc" so it fell through the cracks.                                                                                |
| 4   | **Pre-existing working tree changes are now mixed with my changes**      | 52 files show as modified. At least 6-8 of these (`src/schema-emitter.ts`, `src/minimal-decorators.ts`, `src/state.ts`, `src/state-writers.ts`, `test/integration/decorator-functionality.test.ts`, `test/validation/schema-emitter-regression.test.ts`, `docs/status/2026-07-21_15-52_...`) were modified by a **prior session** and are NOT my work. If committed together, unrelated changes get tangled. | I noted this in the plan ("11 pre-existing modified files exist") but took zero action to isolate my changes. Should have checked `git stash` feasibility or flagged it as a hard blocker before starting. |

---

## e) WHAT WE SHOULD IMPROVE

### Process improvements (how I work)

| #   | Improvement                                                                      | Why                                                                                                                             |
| --- | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Always end with `rg '<pattern>' .` from REPO ROOT, not scoped paths**          | The scoped verification created a false sense of completeness. Full-repo grep is 1 command, takes 1 second, catches everything. |
| 2   | **Never trust a plan's file enumeration as exhaustive**                          | Plans are written with best-guess scope. Execution must independently verify with grep, not check boxes.                        |
| 3   | **"Zero references" claims must be backed by a root-level grep**                 | If I claim "zero X remain", I must have run `rg X .` not `rg X scoped_paths/`.                                                  |
| 4   | **Todo list should include "update planning doc" and "run coverage gate"**       | These are session-completion tasks, not feature tasks, but they're still required for honest completion.                        |
| 5   | **Check working tree cleanliness BEFORE starting**                               | If `git status` shows files I didn't change, that's a blocker or at minimum a documented risk. Mixing changes is sloppy.        |
| 6   | **Run coverage gate as part of verification, not just tests**                    | AGENTS.md says 75% per-file minimum via `scripts/coverage-gate.ts`. I didn't run it.                                            |
| 7   | **Audit customer-facing directories explicitly: examples/, CHANGELOG.md, docs/** | These are where users look first. They deserve their own grep pass.                                                             |

### Codebase improvements (what to fix)

| #   | Improvement                                                                                                                                    | Priority                            |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| 1   | Consolidate `ASYNCAPI_VERSION` (dead, `constants/index.ts`) and `ASYNCAPI_SPEC_VERSION` (live, `document-builder.ts`) into ONE source of truth | **HIGH** — this is a split brain    |
| 2   | The dead constants in `constants/index.ts` (`ASYNCAPI_VERSIONS`, `DEFAULT_CONFIG`) are never imported — delete them or wire them up            | **MEDIUM** — dead code is dishonest |
| 3   | `src/lib.ts:9` diagnostic `"invalid-asyncapi-version"` is defined but never fired — verify it's wired or remove                                | **LOW** — dead diagnostic           |
| 4   | 52 files in working tree with mixed changes — need to isolate and commit in logical units                                                      | **HIGH** — hygiene crisis           |

---

## f) Up to 50 Things to Get Done Next

Sorted by priority (P0 = blocking, P1 = high, P2 = medium, P3 = low).

### P0 — Blocking (must do before declaring upgrade complete)

1. **Update `examples/` directory** — 9 files, 13 references to "3.0": `multi-channel/README.md`, `kafka/README.md`, `simple/README.md`, `simple/expected-output.yaml`, `smoke/main.tsp`, `smoke/README.md`, `real-world/kafka-events.tsp`, `real-world/README.md` (4 refs), `advanced/advanced-decorators.tsp`
2. **Update `CHANGELOG.md`** — 3 references to "3.0"
3. **Run `rg '3\.0\|3\.0\.0\|AsyncAPI 3\.0' .` from repo root** — verify ZERO hits (excluding known false positives)
4. **Update planning doc status** — `docs/planning/2026-07-21_15-22_ASYNCAPI-3.1.0-UPGRADE.md:4` → `**Status:** EXECUTED (2026-07-21)`

### P1 — High priority

5. **Isolate pre-existing working tree changes from upgrade changes** — `git stash`, `git stash pop` strategy or separate commits
6. **Investigate `docs/status/2026-07-21_15-52_TYPE-SAFETY-AND-PROTOCOL-CORRECTNESS.md`** — unknown status doc created 4 min before this session, not authored by me
7. **Run coverage gate** — `bun run scripts/coverage-gate.ts` or equivalent (75% per-file minimum)
8. **Manually inspect a generated 3.1.0 document** — compile `examples/simple/main.tsp`, verify `asyncapi: "3.1.0"` in output
9. **Consolidate version constants** — merge `ASYNCAPI_SPEC_VERSION` (live) and `ASYNCAPI_VERSION` (dead) into single source of truth (separate PR)
10. **Delete dead constants** — `ASYNCAPI_VERSIONS`, `DEFAULT_CONFIG` in `constants/index.ts` if truly unused (verify with `lsp_references` first)

### P2 — Medium priority

11. **Diff 3.0.0.json vs 3.1.0.json schemas** — confirm the ONLY change is ROS 2 bindings + version const, no new required fields
12. **Verify the `invalid-asyncapi-version` diagnostic in `lib.ts` is actually wired** — or remove it as dead code
13. **Check if BDD cucumber tests actually run** — `bun test` runs bun:test, not cucumber; are the `.feature` files executed by any test runner?
14. **Audit all test string literals for "3.0"** — not just `toBe()` calls but `describe()`, `it()`, error messages, comments
15. **Add a test that asserts `ASYNCAPI_SPEC_VERSION` matches the type literal** — prevent future atomic-pair drift at compile time
16. **Consider a single `AsyncAPIVersion` branded type** instead of a string literal in the type definition
17. **Check `.github/workflows/` for hardcoded version references** — CI configs may mention 3.0
18. **Update AGENTS.md "Quick Start" section** — mention 3.1.0 in the project description if not already (verify L3 is the only ref)
19. **Review `examples/smoke/main.tsp`** — this is a .tsp source file with a 3.0 ref; might affect smoke tests

### P3 — Low priority / future

20. **Add ROS 2 protocol to `src/constants/protocols.ts`** — 3.1.0 adds ROS 2 bindings; users currently can't use `@protocol("ros2")` (separate feature)
21. **Consider multi-version support** — emit either 3.0.0 or 3.1.0 based on config (probably YAGNI, but evaluate)
22. **Add a `version.ts` single-source-of-truth file** — export `ASYNCAPI_SPEC_VERSION`, `SUPPORTED_VERSIONS`, type `AsyncAPIVersion` from one place
23. **Write an ADR** — document why we're on 3.1.0 without bumping `@asyncapi/specs` (malware context is non-obvious)
24. **Add the malware context to AGENTS.md** — future agents need to know why we CAN'T bump `@asyncapi/specs` past 6.11.1
25. **Consider a `pre-push` hook** — run `rg '3\.0\.0' src/ test/` to catch version drift
26. **Review the 749-line `asyncapi-generation.test.ts`** — has TODO comments about splitting it
27. **Check `test/bdd/package.json` cucumber version** — `^13.0.0` is fine but verify it's the latest major
28. **Audit all `bindingVersion` values in tests** — ensure they match the 3.1.0 schema's expected binding versions
29. **Verify `@asyncapi/parser@3.6.0` is still the ceiling** — recheck npm for newer safe versions
30. **Add a "version target" field to the emitter options** — let users see what version they're targeting at runtime
31. **Update `.gitignore` if needed** — ensure generated output in examples/ isn't ignored
32. **Run `bun pm audit`** — check for any security advisories on current deps
33. **Review whether the golden file needs regeneration** — did I update the version line only, or does the full golden need regen?
34. **Check `tsconfig.json` for any version references**
35. **Verify the `docs/_archive/` exclusion is still valid** — old docs may reference 3.0 but should stay archived
36. **Consider a version bump in `package.json` version field** — is this a minor or patch bump for this project?
37. **Review all npm scripts in `package.json`** — ensure none reference 3.0
38. **Check if there's a `bun.lock` or `bun.lockb` with stale version refs** — probably fine but verify
39. **Update any IDE config files** (`.vscode/`, `.idea/`) that might reference 3.0
40. **Review the `AGENTS.md` line count** — with 3.1 updates, ensure it's still concise
41. **Consider extracting version to a generated file** — `src/generated/version.ts` with `export const ASYNCAPI_SPEC_VERSION = "3.1.0"` sourced from package.json
42. **Add a CI check for version consistency** — type literal === runtime constant === docs
43. **Review `examples/advanced/advanced-decorators.tsp`** — understand what 3.0 ref it has and update correctly
44. **Check `examples/real-world/kafka-events.tsp`** — same, ensure .tsp source update is correct
45. **Document the `$ref` chain for 3.1.0** — confirm it's identical to 3.0.0 (the plan assumes this)
46. **Review whether any `@bindings` examples need ROS 2 update** — probably not, but check
47. **Consider a "migration guide" for users** — what changes for them going 3.0→3.1? (Answer: nothing, it's additive)
48. **Verify the OAuth2 `availableScopes` structure is unchanged in 3.1.0** — plan assumes yes
49. **Run `bun run build:clean` or equivalent** — full clean rebuild to catch any cached artifacts
50. **Celebrate** — 406 tests pass, the core upgrade IS functionally complete; the misses are documentation, not functionality

---

## g) Questions I CANNOT Answer Myself

### Q1: What do I do with the 6-8 pre-existing modified files in the working tree?

Files like `src/schema-emitter.ts`, `src/minimal-decorators.ts`, `src/state.ts`, `test/validation/schema-emitter-regression.test.ts` (new file), and `docs/status/2026-07-21_15-52_TYPE-SAFETY-AND-PROTOCOL-CORRECTNESS.md` were modified by a **prior session** and are mixed with my upgrade changes. The AGENTS.md global rules say _"NEVER revert changes you didn't author"_ and _"Respect existing changes."_

**Options I see (your call):**

- (a) Commit everything together (tangled but simple)
- (b) `git stash` my changes, commit the pre-existing ones first, then `git stash pop` and commit mine
- (c) Use `git add -p` to selectively stage only my files
- (d) Leave everything uncommitted and you'll sort it

I cannot decide this because I don't know if the pre-existing changes are ready to commit or work-in-progress from another session.

### Q2: Should `examples/` `.tsp` source files have their 3.0 references updated?

`examples/smoke/main.tsp`, `examples/real-world/kafka-events.tsp`, and `examples/advanced/advanced-decorators.tsp` contain "3.0" references. I need to read these files to understand what the references ARE (comments? inline string literals? decorator args?) before changing them. Some `.tsp` references might be in `@doc("...3.0...")` annotations where changing them is cosmetic vs. structural.

**Should I:** read each file and update, OR leave examples/ for a follow-up since the plan didn't scope them?

### Q3: Is there a separate test runner for the BDD `.feature` files?

I updated `test/bdd/features/core-typespec-to-asyncapi.feature` and `test/bdd/support/world.ts`, but `bun test` only runs `bun:test` files (like `test/bdd/user-behaviors.test.ts`). The Cucumber `.feature` files may need a separate runner (`bunx cucumber-js` or similar). I don't know if these BDD tests are actually executed in CI or if they're orphaned.

**Is there a BDD test command I should run, or are the `.feature` files aspirational/inactive?**

---

## Session Metrics

| Metric                                 | Value                                                            |
| -------------------------------------- | ---------------------------------------------------------------- |
| Tasks planned                          | 19                                                               |
| Tasks executed                         | 19/19 (100%)                                                     |
| Tasks that were correctly scoped       | ~15/19 (79%)                                                     |
| Files modified by me                   | ~38                                                              |
| Pre-existing modified files (not mine) | ~8-10                                                            |
| Tests passing                          | 406/406 (100%)                                                   |
| References updated                     | ~180+                                                            |
| References MISSED                      | 16+ (examples/ 13, CHANGELOG.md 3)                               |
| Build                                  | PASS                                                             |
| Lint                                   | PASS                                                             |
| Coverage gate                          | NOT RUN                                                          |
| Committed                              | NO (correctly deferred)                                          |
| Honest completion claim                | **NO** — examples/ and CHANGELOG.md make "zero references" false |

---

## Verschlimmbessern Checklist (from plan) — Actual Results

| Check                                                 | Result                                                  |
| ----------------------------------------------------- | ------------------------------------------------------- |
| Did NOT break the type system (atomic pair)           | ✅ PASS — compiled first try                            |
| Did NOT change any dependency                         | ✅ PASS — `@asyncapi/specs` still 6.11.1                |
| Did NOT implement ROS 2 bindings                      | ✅ PASS — correctly out of scope                        |
| Did NOT change the `$ref` chain                       | ✅ PASS — unchanged                                     |
| Did NOT change OAuth2/security structure              | ✅ PASS — unchanged                                     |
| Did NOT miss any `toBe("3.0.0")`                      | ✅ PASS — zero hits in test/                            |
| **Did NOT leave stale "3.0" in customer-facing docs** | ❌ **FAIL** — examples/ and CHANGELOG.md still have 3.0 |
| Build passes, lint passes, ALL tests pass             | ✅ PASS                                                 |

---

_Generated by brutal self-review. The upgrade works. The documentation is incomplete. The truth matters more than the box-checking._

---

## Resolution (2026-07-22)

### P0 items (section f) — all resolved

| #   | Item                                                     | Resolution                                                     |
| --- | -------------------------------------------------------- | -------------------------------------------------------------- |
| 1   | Update `examples/` directory (9 files, 13 refs to "3.0") | DONE — commit `7376d34`                                        |
| 2   | Update `CHANGELOG.md` (3 refs to "3.0")                  | DONE — commit `7376d34`                                        |
| 3   | Run `rg '3\.0' .` from repo root                         | DONE — only false positive remains (`bindingVersion: "0.3.0"`) |
| 4   | Update planning doc status                               | DONE — both planning docs annotated                            |

### Codebase improvements (section e) — resolved

| #   | Item                                                                   | Resolution                                                                                                  |
| --- | ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| 1   | Consolidate `ASYNCAPI_VERSION` (dead) + `ASYNCAPI_SPEC_VERSION` (live) | DONE — `src/constants/index.ts` deleted entirely; single source in `document-builder.ts` (commit `42ad7ac`) |
| 2   | Delete dead constants in `constants/index.ts`                          | DONE — same commit, file deleted                                                                            |
| 3   | `invalid-asyncapi-version` diagnostic dead?                            | RESOLVED — diagnostic registry unified to 17 codes, all declared and fired (commit `42ad7ac`)               |
| 4   | 52 files with mixed changes                                            | RESOLVED — committed in logical units across `f5088c4`, `7376d34`, `42ad7ac`, `60b526c`, `28bed42`          |
