asyncapi: 3.0.0
info:
  title: Generated API
  version: 1.0.0
  description: API generated from TypeSpec

channels:
  test.messages:
    description: "Channel for publishTestMessage operation"
    publish:
      operationId: "publishTestMessage"
      description: "Publish operation for publishTestMessage"

messages:


components:
  schemas:


