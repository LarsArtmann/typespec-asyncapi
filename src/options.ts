import type { AsyncAPIEmitterOptions } from "./types/options.js";

// Simplified options schema without JSON Schema for now
export type { AsyncAPIEmitterOptions };

export const AsyncAPIEmitterOptionsSchema = {} as any; // TODO: Add proper JSON schema