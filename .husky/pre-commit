#!/bin/sh

# Pre-commit quality gate — full verify: build, lint, test, coverage, duplication

echo "Running full quality gate (pnpm run verify)..."

pnpm run verify || {
    echo "Quality gate failed! Fix errors before committing."
    exit 1
}

echo "All quality checks passed."
