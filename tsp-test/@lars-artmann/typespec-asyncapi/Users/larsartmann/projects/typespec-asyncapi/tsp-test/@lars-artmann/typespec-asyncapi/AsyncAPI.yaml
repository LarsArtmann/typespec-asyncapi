asyncapi: 3.0.0
info:
  title: AsyncAPI Specification
  version: 1.0.0
  description: Generated from TypeSpec with 1 operations, 0 messages, 0 security configs
channels:
  channel_sendMessage:
    address: test.messages
    description: Channel for sendMessage
    messages:
      sendMessageMessage:
        $ref: "#/components/messages/sendMessageMessage"
operations:
  sendMessage:
    action: send
    channel:
      $ref: "#/channels/channel_sendMessage"
    summary: Operation sendMessage
    description: TypeSpec operation with 0 parameters
components:
  schemas:
    SimpleMessage:
      type: object
      description: Schema for SimpleMessage
      properties:
        id:
          description: Property id
          type: string
        content:
          description: Property content
          type: string
      required:
        - id
        - content
  messages:
    sendMessageMessage:
      name: sendMessageMessage
      title: sendMessage Message
      summary: Message for sendMessage operation
      contentType: application/json
      payload:
        $ref: "#/components/schemas/SimpleMessage"
  securitySchemes: {}
