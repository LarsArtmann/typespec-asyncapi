asyncapi: 3.0.0
info:
  title: AsyncAPI Specification
  version: 1.0.0
  description: Generated from TypeSpec with 1 operations, 0 messages, 0 security configs
servers: {}
channels:
  channel_testMessage:
    address: test.simple
    description: Channel for testMessage
    messages:
      testMessageMessage:
        $ref: "#/components/messages/testMessageMessage"
operations:
  testMessage:
    action: send
    channel:
      $ref: "#/channels/channel_testMessage"
    summary: Operation testMessage
    description: TypeSpec operation with 0 parameters
components:
  schemas: {}
  messages:
    testMessageMessage:
      name: testMessageMessage
      title: testMessage Message
      summary: Message for testMessage operation
      contentType: application/json
      payload:
        type: string
  securitySchemes: {}
