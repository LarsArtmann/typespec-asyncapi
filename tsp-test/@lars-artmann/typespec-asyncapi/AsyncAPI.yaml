asyncapi: 3.0.0
info:
  title: AsyncAPI Specification
  version: 1.0.0
  description: Generated from TypeSpec with 1 operations, 0 messages, 0 security configs
channels:
  channel_publishEvent:
    address: events
    description: Publish operation - should create a channel and operation
    messages:
      publishEventMessage:
        $ref: "#/components/messages/publishEventMessage"
operations:
  publishEvent:
    action: send
    channel:
      $ref: "#/channels/channel_publishEvent"
    summary: Operation publishEvent
    description: TypeSpec operation with 0 parameters
components:
  schemas:
    Event:
      type: object
      description: A simple event model
      properties:
        id:
          description: Property id
          type: string
        timestamp:
          description: Property timestamp
          type: string
          format: date-time
      required:
        - id
        - timestamp
  messages:
    publishEventMessage:
      name: publishEventMessage
      title: publishEvent Message
      summary: Message for publishEvent operation
      contentType: application/json
      payload:
        $ref: "#/components/schemas/Event"
  securitySchemes: {}
