asyncapi: 3.0.0
info:
  title: AsyncAPI Specification
  version: 1.0.0
  description: Generated from TypeSpec with 1 operations, 0 messages, 0 security configs
servers: {}
channels:
  channel_publishTestUser:
    address: debug.test
    description: Channel for publishTestUser
    messages:
      publishTestUserMessage:
        $ref: "#/components/messages/publishTestUserMessage"
operations:
  publishTestUser:
    action: send
    channel:
      $ref: "#/channels/channel_publishTestUser"
    summary: Operation publishTestUser
    description: TypeSpec operation with 0 parameters
components:
  schemas:
    TestUser:
      type: object
      description: Schema for TestUser
      properties:
        id:
          description: Property id
          type: string
        name:
          description: Property name
          type: string
        age:
          description: Property age
          type: integer
          format: int32
        isActive:
          description: Property isActive
          type: boolean
      required:
        - id
        - name
        - age
        - isActive
  messages:
    publishTestUserMessage:
      name: publishTestUserMessage
      title: publishTestUser Message
      summary: Message for publishTestUser operation
      contentType: application/json
      payload:
        $ref: "#/components/schemas/TestUser"
  securitySchemes: {}
