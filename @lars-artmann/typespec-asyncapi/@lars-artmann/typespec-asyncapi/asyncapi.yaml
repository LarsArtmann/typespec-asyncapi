asyncapi: 3.0.0
info:
  title: AsyncAPI Specification
  version: 1.0.0
  description: Generated from TypeSpec with 1 operations, 0 messages, 0 security configs
channels:
  channel_publishBasicEvent:
    address: test.basic
    description: Channel for publishBasicEvent
    messages:
      publishBasicEventMessage:
        $ref: "#/components/messages/publishBasicEventMessage"
operations:
  publishBasicEvent:
    action: send
    channel:
      $ref: "#/channels/channel_publishBasicEvent"
    summary: Operation publishBasicEvent
    description: TypeSpec operation with 0 parameters
components:
  schemas:
    BasicEvent:
      type: object
      description: Schema for BasicEvent
      properties:
        id:
          description: Property id
          type: string
        timestamp:
          description: Property timestamp
          type: string
          format: date-time
        data:
          description: Property data
          type: string
      required:
        - id
        - timestamp
        - data
  messages:
    publishBasicEventMessage:
      name: publishBasicEventMessage
      title: publishBasicEvent Message
      summary: Message for publishBasicEvent operation
      contentType: application/json
      payload:
        $ref: "#/components/schemas/BasicEvent"
  securitySchemes: {}
